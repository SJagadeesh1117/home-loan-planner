import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jagadeesh.homeloanplanner',
  appName: 'Home Loan Planner',
  webDir: 'out'
};

export default config;
